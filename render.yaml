services:
  - type: web
    name: nlp-dashboard
    runtime: docker
    dockerfilePath: ./Dockerfile
    buildCommand: ""
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
